User-agent: *
Allow: /

# Old URL pattern (/lang/articles/) now has redirect pages
# Allow crawling so Google can follow the meta refresh redirects

# Prevent RSS feed from appearing in search results
Disallow: /feed.xml

# Block filter parameter URLs (duplicate content)
Disallow: /*?filter=
Disallow: /*&filter=

Sitemap: https://blog.signalpilot.io/sitemap.xml
